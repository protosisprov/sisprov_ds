function inicio(strArrayMenu, strArrayMenuOpt, strArrayOptRol) {
    menu(strArrayMenu);
    var obj=new app();
    id_sistema(obj.name);
    optMenuHave(strArrayMenuOpt);
    putOptRol(strArrayOptRol);
}